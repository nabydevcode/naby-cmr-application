<Directory /var/www/naby-cmr-application/public>
    # Bloque l'accès aux fichiers .env, .git, etc.
    <FilesMatch "^(\.env|\.git|\.htaccess|composer\.json|composer\.lock|package\.json|package-lock\.json|yarn\.lock)$">
        Require all denied
    </FilesMatch>
</Directory>
