{% extends 'base.html.twig' %}


{% block title %}Hello MainController!
{% endblock %}

{% block body %}
	{{ form_start(form) }}
	<div class="space-y-2">
		{% for choice in form.roles %}
			<label class="flex items-center space-x-2">
				{{ form_widget(choice) }}
				<span class="bg-blue-100 text-blue-800 px-3 py-1 rounded-full text-sm font-semibold">
					{{ form_label(choice) }}
				</span>
			</label>
		{% endfor %}

	</div>

	<button class="w-full bg-gray-900 text-white py-2 px-4 rounded-lg font-semibold hover:bg-gray-600 transition duration-300">
		Mettre à jour
	</button>
	{{ form_end(form) }}
{% endblock %}
