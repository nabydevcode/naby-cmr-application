{% extends 'base.html.twig' %}


{% block title %}Hello MainController!
{% endblock %}

{% block body %}


	<div class="min-h-screen flex items-start justify-center bg-gray-100 pt-10 ">
		<div class="bg-white rounded-lg shadow-xl p-8 max-w-lg w-full">
			<h1 class="text-3xl font-bold text-center mb-6">Formulaire de données</h1>
			<div class="grid grid-cols-3 gap-4">
				<a href="{{ path('app_main_formulaire') }}" class="bg-gray-800 hover:bg-gray-900 text-white py-3 px-4 rounded-lg text-center transition">
					Creer CMR
				</a>
				<a href="{{path('app_transporteur')}}" class="bg-gray-800 hover:bg-gray-900 text-white py-3 px-4 rounded-lg text-center transition">
					Transporteur
				</a>
				<a href="{{path('app_consigne')}}" class="bg-gray-800 hover:bg-gray-900 text-white py-3 px-4 rounded-lg text-center transition">
					Destinataire
				</a>
				<a href="{{path('app_delivery_location')}}" class="bg-gray-800 hover:bg-gray-900 text-white py-3 px-4 rounded-lg text-center transition">
					Lieu-Livraison
				</a>
				<a href="{{path('app_recherche')}}" class="bg-gray-800 hover:bg-gray-900 text-white py-3 px-4 rounded-lg text-center transition">
					Rechercher
				</a>
				<a href="{{path('app_company')}}" class="bg-gray-800 hover:bg-gray-900 text-white py-3 px-6 rounded-lg text-center transition">
					Expediteur
				</a>
				<a href="{{path('app_loading_location')}}" class="bg-gray-800 hover:bg-gray-900 text-white py-3 px-6 rounded-lg text-center transition">
					Lieux de Chargment
				</a>
				<a href="{{path('app_type_loading')}}" class="bg-gray-800 hover:bg-gray-900 text-white py-3 px-6 rounded-lg text-center transition">
					Type de Chargement
				</a>
				<a href="{{path('main_plomb')}}" class="bg-gray-800 hover:bg-gray-900 text-white py-3 px-6 rounded-lg text-center transition">
					Plomb
				</a>
				{% if  is_granted('ROLE_ADMIN') %}

					<a href="{{path('general_plomb')}}" class="bg-gray-800 hover:bg-gray-900 text-white py-3 px-6 rounded-lg text-center transition">
						Tous les Plomb
					</a>
				{% endif %}
				{% if  is_granted('ROLE_ADMIN') %}

					<a href="{{path('app_dasabord')}}" class="bg-gray-800 hover:bg-gray-900 text-white py-3 px-6 rounded-lg text-center transition">
						Dashboard
					</a>
				{% endif %}

			</div>
		</div>
	</div>
{% endblock %}
